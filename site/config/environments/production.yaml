# Production Environment
# =======================

site:
  # GitHub Pages project site: deployed at /pounce
  baseurl: "/pounce"

build:
  incremental: true
  strict_mode: true

assets:
  minify: true
  fingerprint: true
