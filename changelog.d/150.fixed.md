Packaged a `chirp` install extra (`pip install bengal-pounce[chirp]`), folded `bengal-chirp>=0.7.1` into the `full` extra, and added it to the dev dependency-group so the chirp example/compat tests now actually run in CI instead of being skipped. ([#150](https://github.com/lbliii/pounce/issues/150))
