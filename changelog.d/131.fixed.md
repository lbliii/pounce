Honor `Accept-Encoding` q-values when selecting precompressed (`.gz`/`.zst`) static assets (so e.g. `gzip;q=0` disables gzip) and always emit `Vary: Accept-Encoding` when content-encoding negotiation occurs.
