The `--worker-mode` CLI help now marks `subinterpreter` as `(beta)`, and the server emits a one-line INFO notice at startup when the beta subinterpreter worker mode is actually resolved. The `core-contract` design doc gained an "Observability Name Contract" section enumerating the stable Prometheus metric names, lifecycle event names, and structured/access-log field names.
