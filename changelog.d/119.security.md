Reject requests with duplicate `Host` headers in the sync fast HTTP/1.1 parser (`POUNCE_PARSE_DUPLICATE_HOST` -> 400), closing a request-smuggling / routing-desync gap and matching the async (h11) path.
