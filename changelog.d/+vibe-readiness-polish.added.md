Vibe-readiness polish — wire the last-mile UX for the `POUNCE_` code surface. `_die` / `_output.error` now render `Code:` and `See:` lines in both pretty and plain-text modes, and `PounceError.doc` auto-derives from `code` so every raise site resolves to `docs/troubleshooting.md#<code>` without per-site churn. CLI fixes: `pounce config --help` lists its `schema` / `show` subcommands, `pounce check` prints per-check PASS/FAIL lines + a summary on success, `pounce check --config FILE` achieves parity with `serve`, `pounce init` no longer prints a trailing `None`, and the scaffolded `app.py` docstring now signposts every other pounce command. Adds the `/_pounce/info` introspection endpoint (opt-in via `introspection_enabled`; loopback-bound by default; allowlist-redacted via `INFO_ALLOWLIST`) and emits `POUNCE_CONFIG_INTROSPECTION_PUBLIC` at startup when bound non-loopback.
