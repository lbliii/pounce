Vibe-coding readiness: structured error codes, config inspection, and project scaffolding. `PounceError` now carries `code`/`hint`/`doc` kwargs, preserved across pickle; 22 raise sites and 7 `_send_error` sites emit canonical `POUNCE_<CATEGORY>_<SPECIFIC>` codes surfaced via the `X-Pounce-Error-Code` response header. New CLI subcommands: `pounce config schema` (JSON Schema / commented TOML), `pounce config show` (resolved config through a fail-closed redaction allowlist), and `pounce init` (scaffolds `app.py` + `pounce.toml` + `.gitignore`). Adds `CONTRIBUTING.md`, `docs/troubleshooting.md` error-code catalog (AST-enforced coverage), and four ADRs in `docs/design/`.
