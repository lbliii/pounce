Harden leaf-module correctness under free-threaded Python 3.14t: per-worker RequestQueue (shared asyncio primitives drifted across event loops), RFC 9218-conformant HTTP/2 priority scheduler wired through the H2 bridge, CRLF guard in the response framer (RFC 9110 §5.5), snapshot-then-probe rate-limiter cleanup, IPv6/UNIX socket support and config-driven timeouts in the subinterpreter bootstrap, and several lower-impact correctness/performance cleanups (`_debug` HTML builder, `_profile` p99, `logging` public helpers, `lifecycle.monotonic_ns` single source).
