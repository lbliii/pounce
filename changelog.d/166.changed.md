Test runs now apply a default per-test timeout (`--timeout=60 --timeout-method=thread`) so a hung socket or drain test fails fast with a per-test traceback instead of stalling the whole job. The local `test-fast` task still overrides with a tighter `--timeout=10`.
