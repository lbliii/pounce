Return `416 Range Not Satisfiable` (with `Content-Range: bytes */<size>`) for unsatisfiable byte ranges, clamp ranges that extend past end-of-file, and cap/coalesce multi-range requests to bound response amplification.
