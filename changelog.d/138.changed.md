Document the JSON access-log line as a stability contract (exact key set, value types, and `req_id` policy) and emit the request id **in full** in both `json` and `text` modes, so the access-log `req_id` matches the `X-Request-ID` response header byte-for-byte (previously truncated to 8/12 chars, which broke exact correlation and disagreed between formats).
