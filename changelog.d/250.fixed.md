Serve built-in health, introspection, and compression-dictionary endpoints for `HEAD` with GET-equivalent status and headers but no response body; document the endpoint as `/readyz` readiness with separate application-owned `/healthz` liveness.
