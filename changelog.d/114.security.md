Sanitize WebSocket rejection response headers and coerce the rejection status to an integer, closing a CRLF header-injection vector on the WebSocket reject path (parity with the HTTP/1.1, HTTP/2, and HTTP/3 response paths).
