Document the fork-safety contract for GIL-build process workers: application
state created before worker startup must be safe to inherit, while
process-local clients, executors, and synchronization state belong in
`pounce.worker.startup`. The public guidance now names `workers=1` and Python
3.14t thread workers as alternatives; no process start method changed.
