Resolved worker models are now reported in startup output and
`/_pounce/info`; embedded subinterpreter mode requires `app_path` up front and
uses the supervised isolated-worker path even when `workers=1`.
