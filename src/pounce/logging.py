"""
Logging — access logs and error logs.

Provides structured access logging and error reporting for the server.
Access log format follows common conventions (method, path, status, timing).

No structured logging library dependency — uses stdlib logging with
optional formatters.

"""
